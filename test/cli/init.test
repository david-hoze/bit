# Test: bit init

# Setup: clean environment
rm -rf test/cli/output/work_init 2>/dev/null; mkdir -p test/cli/output/work_init && cd test/cli/output/work_init && bit init
<<<
>>> /.*[Ii]nitialized.*/
>>>= 0

# Verify .bit directory was created
test -d test/cli/output/work_init/.bit && echo exists || echo missing
<<<
>>>
exists
>>>= 0
